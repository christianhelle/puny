[
  {
    "name": "Basic text response",
    "args": ["--mock", "--model", "mock-model", "--prompt", "hello world", "--oneshot"],
    "expect": ["mock response", "hello world"],
    "not_expect": []
  },
  {
    "name": "Tool call: read_file",
    "args": ["--mock", "--model", "mock-model", "--prompt", "read the code", "--oneshot"],
    "expect": ["Reading", "Tool executed"],
    "not_expect": []
  },
  {
    "name": "Tool call: grep_search",
    "args": ["--mock", "--model", "mock-model", "--prompt", "search for pattern", "--oneshot"],
    "expect": ["Searching for", "Tool executed"],
    "not_expect": []
  },
  {
    "name": "Tool call: execute_shell",
    "args": ["--mock", "--model", "mock-model", "--prompt", "run a shell command", "--oneshot"],
    "expect": ["Running", "Tool executed"],
    "not_expect": []
  },
  {
    "name": "Error does not produce content",
    "args": ["--mock", "--model", "mock-model", "--prompt", "trigger an error", "--oneshot"],
    "expect": ["Chat failed"],
    "not_expect": ["mock response"]
  },
  {
    "name": "Long text output",
    "args": ["--mock", "--model", "mock-model-long", "--prompt", "write something long", "--oneshot"],
    "expect": ["generates"],
    "not_expect": []
  },
  {
    "name": "Echo mode",
    "args": ["--mock", "--model", "mock-model", "--prompt", "echo hello world", "--oneshot"],
    "expect": ["Echo:", "hello world"],
    "not_expect": []
  },
  {
    "name": "Empty response",
    "args": ["--mock", "--model", "mock-model", "--prompt", "give me an empty response", "--oneshot"],
    "expect": [],
    "not_expect": ["mock response"]
  },
  {
    "name": "Usage stats response",
    "args": ["--mock", "--model", "mock-model", "--prompt", "show usage info", "--oneshot"],
    "expect": ["usage statistics"],
    "not_expect": []
  },
  {
    "name": "Partial response (no finish)",
    "args": ["--mock", "--model", "mock-model", "--prompt", "give me a partial response", "--oneshot"],
    "expect": ["partial response", "truncated stream"],
    "not_expect": []
  },
  {
    "name": "Fast mode (instant)",
    "args": ["--mock", "--model", "mock-model", "--prompt", "respond fast", "--oneshot"],
    "expect": ["mock response"],
    "not_expect": []
  },
  {
    "name": "Slow mode",
    "args": ["--mock", "--model", "mock-model-slow", "--prompt", "respond slowly", "--oneshot"],
    "expect": ["mock response"],
    "not_expect": []
  },
  {
    "name": "Mock flag with explicit provider does not crash",
    "args": ["--mock", "--provider", "lmstudio", "--model", "mock-model", "--prompt", "hello world", "--oneshot"],
    "expect": ["mock response"],
    "not_expect": []
  },
  {
    "name": "Markdown table response",
    "args": ["--mock", "--model", "mock-model", "--prompt", "im testing the markdown table renderer. generate a markdown table with a realistic data set", "--oneshot"],
    "expect": ["Alice Johnson", "Engineering", "employee data"],
    "not_expect": []
  },
  {
    "name": "Complex markdown response",
    "args": ["--mock", "--model", "mock-model", "--prompt", "test complex markdown rendering", "--oneshot"],
    "expect": ["Complex Markdown Sample", "Code Blocks", "Lists", "Blockquotes", "Blockquote with Mixed Content", "embedded code block", "literal backticks"],
    "not_expect": []
  },
  {
    "name": "Chat log creates file with user and assistant labels",
    "args": ["--mock", "--model", "mock-model", "--prompt", "echo hello world", "--oneshot", "--chat-log"],
    "expect": ["Echo:", "hello world"],
    "not_expect": [],
    "evidence": {
      "file_exists": ["puny_chat.log"],
      "file_contains": {
        "path": "puny_chat.log",
        "patterns": ["[USER]", "[ASSISTANT]"]
      }
    }
  },
  {
    "name": "Chat log captures tool calls and results",
    "args": ["--mock", "--model", "mock-model", "--prompt", "read the code", "--oneshot", "--chat-log"],
    "expect": ["Reading", "Tool executed"],
    "not_expect": [],
    "evidence": {
      "file_exists": ["puny_chat.log"],
      "file_contains": {
        "path": "puny_chat.log",
        "patterns": ["[TOOL_CALL]", "[TOOL_RESULT]"]
      }
    }
  }
]
